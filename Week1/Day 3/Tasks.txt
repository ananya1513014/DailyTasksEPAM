1.Create your own custom annotations and include information such as author, version, creation date etc.

2.Write a small program that accepts two, and only two command line arguments. If the first parameter is 0, convert the second parameter from Celsius to Fahrenheit. Otherwise, do the conversion backwards. The way to do the first conversion is the following: C = (F-32) * 5 / 9

3.Write a function which evaluates the sum and avg of an array

4.Write a function which joins the elements of an array into one string. The separator character must be optional.

5.Wite a function that can pretty print a given 2D Array